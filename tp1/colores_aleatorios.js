function Tiempo (t) {//Creo una función propia con parámetro del tipo flotante t, que devuelve valores booleanos.
  if (t>=10) {//Si la variable t es mayor igual a 10, la función Tiempo devuelve valor true.
    return true;
  } else {//De lo contrario, si t<10, la función Tiempo devuelve valor false.
    return false;
  }
}
